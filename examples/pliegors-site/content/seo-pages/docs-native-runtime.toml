key = "docs-native-runtime"
title = { en = "Native Runtime Preview | PliegoRS G1", es = "Preview del Runtime Nativo | PliegoRS G1" }
description = { en = "Evaluate the released PliegoRS native runtime preview: route-owned layouts, operator-enabled OpenTelemetry, bounded HTTP lifecycle, receipts, and three SSR modes.", es = "Evalúa el preview liberado del runtime nativo de PliegoRS: layouts controlados por rutas, OpenTelemetry habilitado por el operador, lifecycle HTTP limitado, recibos y tres modos SSR." }
image = "/media/pliegors/ledger-wide.webp"
imageAlt = { en = "A precision ledger representing the bounded PliegoRS native runtime", es = "Un ledger de precisión que representa el runtime nativo limitado de PliegoRS" }
