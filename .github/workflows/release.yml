name: Build and seal PliegoRS release

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Exact Cargo version tag to prepare, for example v0.0.1
        required: true
        type: string
      mode:
        description: Build a sealed candidate or create a reviewable draft from main
        required: true
        default: candidate
        type: choice
        options:
          - candidate
          - draft
      channel:
        description: Promotion channel represented by these exact bytes
        required: true
        default: canary
        type: choice
        options:
          - canary
          - beta
          - stable
      confirmation:
        description: Type candidate:<tag> or draft:<tag>
        required: true
        type: string
      wsl_report_base64:
        description: Base64 P8 WSL2 golden report for the same draft commit; optional for canary
        required: false
        default: ''
        type: string

permissions:
  contents: read

concurrency:
  group: pliegors-${{ inputs.mode }}-${{ inputs.tag }}-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    if: >-
      ${{
        (inputs.mode == 'candidate' && inputs.channel == 'canary' && inputs.confirmation == format('candidate:{0}', inputs.tag)) ||
        (github.ref == 'refs/heads/main' && inputs.mode == 'draft' && inputs.channel != 'canary' &&
          inputs.confirmation == format('draft:{0}', inputs.tag))
      }}
    name: Build ${{ matrix.target }} replica ${{ matrix.replica }}
    env:
      CARGO_INCREMENTAL: 0
    runs-on: >-
      ${{
        matrix.target == 'x86_64-unknown-linux-gnu' && 'ubuntu-22.04' ||
        matrix.target == 'aarch64-unknown-linux-gnu' && 'ubuntu-24.04-arm' ||
        matrix.target == 'x86_64-apple-darwin' && 'macos-15-intel' ||
        matrix.target == 'aarch64-apple-darwin' && 'macos-15' ||
        'windows-2025'
      }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        target:
          - x86_64-unknown-linux-gnu
          - aarch64-unknown-linux-gnu
          - x86_64-apple-darwin
          - aarch64-apple-darwin
          - x86_64-pc-windows-msvc
        replica: [1, 2]
    steps:
      - name: Check out the exact candidate source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          ref: ${{ github.sha }}

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.86.0 --profile minimal --target ${{ matrix.target }}

      - name: Verify request and distribution source contract
        shell: pwsh
        env:
          RELEASE_CONFIRMATION: ${{ inputs.confirmation }}
          RELEASE_CHANNEL: ${{ inputs.channel }}
          RELEASE_MODE: ${{ inputs.mode }}
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          npm run check:distribution
          $version = cargo metadata --no-deps --format-version 1 | ConvertFrom-Json |
            Select-Object -ExpandProperty packages |
            Where-Object name -eq 'pliego-cli' |
            Select-Object -ExpandProperty version
          $expectedTag = "v$version"
          if ($env:RELEASE_TAG -cne $expectedTag) {
            throw "release tag $env:RELEASE_TAG does not match Cargo version $expectedTag"
          }
          if ($env:RELEASE_CONFIRMATION -cne "$env:RELEASE_MODE`:$expectedTag") {
            throw "confirmation must be $env:RELEASE_MODE`:$expectedTag"
          }
          $isPrerelease = $version.Contains('-')
          if ($env:RELEASE_MODE -eq 'candidate' -and $env:RELEASE_CHANNEL -cne 'canary') {
            throw 'candidate mode is the canary channel'
          }
          if ($env:RELEASE_MODE -eq 'draft' -and $env:RELEASE_CHANNEL -eq 'canary') {
            throw 'draft mode requires the beta or stable channel'
          }
          if ($env:RELEASE_CHANNEL -eq 'beta' -and -not $isPrerelease) {
            throw 'beta channel requires a prerelease SemVer tag'
          }
          if ($env:RELEASE_CHANNEL -eq 'stable' -and $isPrerelease) {
            throw 'stable channel rejects prerelease SemVer tags'
          }

      - name: Build reproducible release binary
        shell: pwsh
        run: |
          $flags = "-C strip=symbols -C debuginfo=0 --remap-path-prefix=$env:GITHUB_WORKSPACE=/workspace/pliegors"
          if ('${{ matrix.target }}' -eq 'x86_64-pc-windows-msvc') {
            $flags = "$flags -C link-arg=/Brepro"
          }
          $env:RUSTFLAGS = $flags
          cargo build --locked --release -p pliego-cli --target '${{ matrix.target }}'

      - name: Smoke test built CLI
        shell: pwsh
        run: |
          $binary = if ('${{ matrix.target }}' -eq 'x86_64-pc-windows-msvc') { 'pliego.exe' } else { 'pliego' }
          & "target/${{ matrix.target }}/release/$binary" version

      - name: Package immutable build replica
        id: package
        shell: pwsh
        env:
          REPLICA: ${{ matrix.replica }}
          RELEASE_TARGET: ${{ matrix.target }}
        run: |
          $version = cargo metadata --no-deps --format-version 1 | ConvertFrom-Json |
            Select-Object -ExpandProperty packages |
            Where-Object name -eq 'pliego-cli' |
            Select-Object -ExpandProperty version
          $binary = if ($env:RELEASE_TARGET -eq 'x86_64-pc-windows-msvc') { 'pliego.exe' } else { 'pliego' }
          $support = if ($env:RELEASE_TARGET -match 'unknown-linux-gnu$') { 'production' } else { 'development' }
          $stem = "pliego-$version-$env:RELEASE_TARGET"
          $archive = "pliego-$env:RELEASE_TARGET.zip"
          $directory = "dist/$env:RELEASE_TARGET-$env:REPLICA"
          New-Item -ItemType Directory -Force "$directory/$stem" | Out-Null
          Copy-Item "target/$env:RELEASE_TARGET/release/$binary" "$directory/$stem/"
          Copy-Item LICENSE, README.md "$directory/$stem/"
          @(
            "version=$version"
            "commit=$env:GITHUB_SHA"
            "target=$env:RELEASE_TARGET"
            "support=$support"
          ) | Set-Content -Encoding utf8NoBOM "$directory/$stem/RELEASE-METADATA.txt"
          node scripts/create-deterministic-zip.mjs `
            --root "$directory/$stem" `
            --output "$directory/$archive" `
            --executable "$stem/$binary"
          $archiveHash = (Get-FileHash "$directory/$archive" -Algorithm SHA256).Hash.ToLowerInvariant()
          $binaryHash = (Get-FileHash "target/$env:RELEASE_TARGET/release/$binary" -Algorithm SHA256).Hash.ToLowerInvariant()
          $archiveBytes = (Get-Item "$directory/$archive").Length
          "$archiveHash  $archive" | Set-Content -Encoding utf8NoBOM -NoNewline "$directory/$archive.sha256"
          $env:CANDIDATE_VERSION = $version
          $env:CANDIDATE_SUPPORT = $support
          $env:CANDIDATE_ARCHIVE = $archive
          $env:CANDIDATE_ARCHIVE_HASH = $archiveHash
          $env:CANDIDATE_ARCHIVE_BYTES = $archiveBytes
          $env:CANDIDATE_BINARY_HASH = $binaryHash
          $env:CANDIDATE_METADATA_PATH = "$directory/CANDIDATE-METADATA.json"
          node -e @'
          const fs = require('node:fs');
          const value = {
            schema: 'dev.pliegors.candidate-build/v1',
            version: process.env.CANDIDATE_VERSION,
            commit: process.env.GITHUB_SHA,
            target: process.env.RELEASE_TARGET,
            support: process.env.CANDIDATE_SUPPORT,
            replica: Number(process.env.REPLICA),
            archive: process.env.CANDIDATE_ARCHIVE,
            archiveSha256: process.env.CANDIDATE_ARCHIVE_HASH,
            archiveBytes: Number(process.env.CANDIDATE_ARCHIVE_BYTES),
            binarySha256: process.env.CANDIDATE_BINARY_HASH,
          };
          fs.writeFileSync(process.env.CANDIDATE_METADATA_PATH, `${JSON.stringify(value, null, 2)}\n`);
          '@
          "directory=$directory" >> $env:GITHUB_OUTPUT

      - name: Verify packaged replica
        shell: pwsh
        run: |
          $directory = '${{ steps.package.outputs.directory }}'
          $metadata = Get-Content -Raw "$directory/CANDIDATE-METADATA.json" | ConvertFrom-Json
          $expected = ((Get-Content -Raw "$directory/$($metadata.archive).sha256") -split '\s+')[0]
          $actual = (Get-FileHash "$directory/$($metadata.archive)" -Algorithm SHA256).Hash.ToLowerInvariant()
          if ($actual -cne $expected -or $actual -cne $metadata.archiveSha256) {
            throw 'candidate archive checksum mismatch'
          }

      - name: Smoke test Unix installer lifecycle
        if: matrix.replica == 1 && runner.os != 'Windows'
        shell: bash
        env:
          PLIEGORS_INSTALLER_ALLOW_UNSEALED: "1"
        run: |
          set -euo pipefail
          directory='${{ steps.package.outputs.directory }}'
          archive="$directory/pliego-${{ matrix.target }}.zip"
          bash scripts/install.sh --archive "$archive" --install-dir target/installer-smoke
          target/installer-smoke/bin/pliego version
          bash scripts/install.sh --archive "$archive" --install-dir target/installer-smoke
          bash scripts/install.sh --rollback --install-dir target/installer-smoke
          target/installer-smoke/bin/pliego version
          bash scripts/install.sh --uninstall --install-dir target/installer-smoke
          test ! -e target/installer-smoke/bin/pliego

      - name: Smoke test Windows installer lifecycle
        if: matrix.replica == 1 && runner.os == 'Windows'
        shell: pwsh
        env:
          PLIEGORS_INSTALLER_ALLOW_UNSEALED: "1"
        run: |
          $directory = '${{ steps.package.outputs.directory }}'
          $installRoot = 'target/installer-smoke'
          ./scripts/install.ps1 -ArchivePath "$directory/pliego-${{ matrix.target }}.zip" -InstallDir $installRoot
          & "$installRoot/bin/pliego.exe" version
          ./scripts/install.ps1 -ArchivePath "$directory/pliego-${{ matrix.target }}.zip" -InstallDir $installRoot
          ./scripts/install.ps1 -Rollback -InstallDir $installRoot
          & "$installRoot/bin/pliego.exe" version
          ./scripts/install.ps1 -Uninstall -InstallDir $installRoot
          if (Test-Path "$installRoot/bin/pliego.exe") { throw 'uninstall left the active binary behind' }

      - name: Upload build replica
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pliegors-build-${{ matrix.target }}-${{ matrix.replica }}-${{ github.sha }}
          path: ${{ steps.package.outputs.directory }}/*
          if-no-files-found: error
          retention-days: 7
          compression-level: 0

  seal:
    if: >-
      ${{
        needs.build.result == 'success' &&
        ((inputs.mode == 'candidate' && inputs.channel == 'canary' && inputs.confirmation == format('candidate:{0}', inputs.tag)) ||
        (github.ref == 'refs/heads/main' && inputs.mode == 'draft' && inputs.channel != 'canary' &&
          inputs.confirmation == format('draft:{0}', inputs.tag)))
      }}
    name: Assemble and sign exact uploaded assets
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Check out immutable assembly sources
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          ref: ${{ github.sha }}
          path: source

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Download all uploaded build replicas
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: pliegors-build-*-${{ github.sha }}
          path: build-assets

      - name: Assemble, sign, and independently verify candidate
        env:
          PLIEGORS_CANDIDATE_SIGNING_KEY: ${{ secrets.PLIEGORS_CANDIDATE_SIGNING_KEY }}
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          version="${RELEASE_TAG#v}"
          source_date_epoch="$(git -C source show -s --format=%ct "$GITHUB_SHA")"
          symlinks="$(git -C source ls-files -s | awk '$1 == "120000" { print $4 }')"
          test -z "$symlinks" || {
            echo "source archive refuses tracked symlinks" >&2
            exit 1
          }
          for replica in 1 2; do
            git -C source archive --format=tar --prefix=pliegors-source/ "$GITHUB_SHA" |
              gzip -n -9 > "pliegors-source-$replica.tar.gz"
          done
          cmp pliegors-source-1.tar.gz pliegors-source-2.tar.gz
          mv pliegors-source-1.tar.gz pliegors-source.tar.gz
          rm pliegors-source-2.tar.gz
          node source/scripts/assemble-release-candidate.mjs \
            --input build-assets \
            --output release-assets \
            --source source \
            --source-archive pliegors-source.tar.gz \
            --public-key source/keys/pliegors-candidate-release.pub.pem \
            --version "$version" \
            --commit "$GITHUB_SHA"
          node source/scripts/create-release-manifest.mjs \
            --dir release-assets \
            --version "$version" \
            --commit "$GITHUB_SHA" \
            --source-date-epoch "$source_date_epoch"
          node source/scripts/verify-release-bundle.mjs \
            --dir release-assets \
            --public-key source/keys/pliegors-candidate-release.pub.pem \
            --expected-key-fingerprint sha256:97df5a29b5d4be6f626634b6824eebea5f2e7fcfa9c93ed644a3a2913dad7250

      - name: Upload sealed candidate
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pliegors-sealed-${{ inputs.tag }}-${{ github.sha }}
          path: release-assets/*
          if-no-files-found: error
          retention-days: 14
          compression-level: 0

  supply_chain:
    if: ${{ needs.seal.result == 'success' }}
    name: Generate and verify supply-chain attestations
    needs: seal
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out immutable attestation sources
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          ref: ${{ github.sha }}
          path: source

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Install pinned Rust toolchain
        run: rustup toolchain install 1.86.0 --profile minimal

      - name: Download sealed candidate
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: pliegors-sealed-${{ inputs.tag }}-${{ github.sha }}
          path: release-assets

      - name: Generate pinned CycloneDX SBOM
        run: |
          set -euo pipefail
          cargo install cargo-cyclonedx --version 0.5.9 --locked
          (
            cd source
            cargo cyclonedx --format json --all --override-filename PLIEGORS
          )
          test -s source/crates/pliego-cli/PLIEGORS.json

      - name: Create SLSA-compatible provenance and exact attestation manifest
        env:
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          version="${RELEASE_TAG#v}"
          node source/scripts/create-supply-chain-attestations.mjs \
            --release release-assets \
            --output attestations \
            --sbom source/crates/pliego-cli/PLIEGORS.json \
            --source source \
            --version "$version" \
            --commit "$GITHUB_SHA" \
            --run-id "$GITHUB_RUN_ID" \
            --workflow-ref "https://github.com/$GITHUB_REPOSITORY/.github/workflows/release.yml@$GITHUB_REF"
      - name: Install pinned cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: v3.1.2

      - name: Sign and verify attestation manifest with GitHub OIDC
        run: |
          set -euo pipefail
          identity="https://github.com/$GITHUB_REPOSITORY/.github/workflows/release.yml@$GITHUB_REF"
          cosign sign-blob \
            --yes \
            --bundle attestations/ATTESTATIONS.sigstore.json \
            attestations/ATTESTATIONS.json
          cosign verify-blob \
            --bundle attestations/ATTESTATIONS.sigstore.json \
            --certificate-identity "$identity" \
            --certificate-oidc-issuer https://token.actions.githubusercontent.com \
            attestations/ATTESTATIONS.json
          node attestations/verify-supply-chain-attestations.mjs \
            --release release-assets \
            --attestations attestations

      - name: Upload verified attestations
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pliegors-attestations-${{ inputs.tag }}-${{ github.sha }}
          path: attestations/*
          if-no-files-found: error
          retention-days: 14
          compression-level: 0

  golden_path:
    if: ${{ needs.seal.result == 'success' && needs.supply_chain.result == 'success' }}
    name: Golden ${{ matrix.environment }}
    needs: [seal, supply_chain]
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - environment: linux-x64
            runner: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            scenario: standard
            container: false
          - environment: linux-arm64
            runner: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            scenario: standard
            container: false
          - environment: macos-x64
            runner: macos-15-intel
            target: x86_64-apple-darwin
            scenario: standard
            container: false
          - environment: macos-arm64
            runner: macos-15
            target: aarch64-apple-darwin
            scenario: standard
            container: false
          - environment: windows-x64
            runner: windows-2025
            target: x86_64-pc-windows-msvc
            scenario: standard
            container: false
          - environment: windows-unicode
            runner: windows-2025
            target: x86_64-pc-windows-msvc
            scenario: unicode
            container: false
          - environment: windows-long-path
            runner: windows-2025
            target: x86_64-pc-windows-msvc
            scenario: long-path
            container: false
          - environment: container-linux-x64
            runner: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            scenario: standard
            container: true
    steps:
      - name: Set up Node.js
        if: ${{ !matrix.container }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Install pinned Rust toolchain
        if: ${{ !matrix.container }}
        run: rustup toolchain install 1.86.0 --profile minimal

      - name: Download sealed candidate only
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: pliegors-sealed-${{ inputs.tag }}-${{ github.sha }}
          path: release-assets

      - name: Download supply-chain attestations
        if: ${{ matrix.environment == 'linux-x64' }}
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: pliegors-attestations-${{ inputs.tag }}-${{ github.sha }}
          path: attestations

      - name: Install pinned cosign verifier
        if: ${{ matrix.environment == 'linux-x64' }}
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: v3.1.2

      - name: Verify supply-chain package without a source checkout
        if: ${{ matrix.environment == 'linux-x64' }}
        run: |
          set -euo pipefail
          test ! -d .git
          identity="https://github.com/$GITHUB_REPOSITORY/.github/workflows/release.yml@$GITHUB_REF"
          node attestations/verify-supply-chain-attestations.mjs \
            --release release-assets \
            --attestations attestations
          cosign verify-blob \
            --bundle attestations/ATTESTATIONS.sigstore.json \
            --certificate-identity "$identity" \
            --certificate-oidc-issuer https://token.actions.githubusercontent.com \
            attestations/ATTESTATIONS.json

      - name: Run native Unix golden path
        if: ${{ runner.os != 'Windows' && !matrix.container }}
        env:
          DEPENDENCY_SOURCE: ${{ inputs.mode == 'candidate' && 'candidate-source' || 'registry' }}
        run: |
          set -euo pipefail
          test ! -d .git
          mkdir -p "$RUNNER_TEMP/golden-reports"
          node release-assets/run-golden-path.mjs \
            --release release-assets \
            --output "$RUNNER_TEMP/golden-reports/${{ matrix.environment }}.json" \
            --environment-id "${{ matrix.environment }}" \
            --target "${{ matrix.target }}" \
            --scenario "${{ matrix.scenario }}" \
            --dependency-source "$DEPENDENCY_SOURCE"

      - name: Run native Windows golden path
        if: ${{ runner.os == 'Windows' }}
        shell: pwsh
        env:
          DEPENDENCY_SOURCE: ${{ inputs.mode == 'candidate' && 'candidate-source' || 'registry' }}
        run: |
          if (Test-Path .git) { throw 'golden path unexpectedly has a checkout' }
          $reports = Join-Path $env:RUNNER_TEMP 'golden-reports'
          New-Item -ItemType Directory -Force -Path $reports | Out-Null
          node release-assets/run-golden-path.mjs `
            --release release-assets `
            --output (Join-Path $reports '${{ matrix.environment }}.json') `
            --environment-id '${{ matrix.environment }}' `
            --target '${{ matrix.target }}' `
            --scenario '${{ matrix.scenario }}' `
            --dependency-source $env:DEPENDENCY_SOURCE

      - name: Run pinned clean container golden path
        if: ${{ matrix.container }}
        env:
          DEPENDENCY_SOURCE: ${{ inputs.mode == 'candidate' && 'candidate-source' || 'registry' }}
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/golden-reports"
          docker run --rm \
            -v "$PWD/release-assets:/release:ro" \
            -v "$RUNNER_TEMP/golden-reports:/reports" \
            rust:1.86.0-slim-bookworm@sha256:57d415bbd61ce11e2d5f73de068103c7bd9f3188dc132c97cef4a8f62989e944 \
            bash -euo pipefail -c '
              apt-get update
              apt-get install -y --no-install-recommends nodejs unzip
              rm -rf /var/lib/apt/lists/*
              node /release/run-golden-path.mjs \
                --release /release \
                --output /reports/container-linux-x64.json \
                --environment-id container-linux-x64 \
                --target x86_64-unknown-linux-gnu \
                --scenario standard \
                --dependency-source "$1"
              chmod 0644 /reports/container-linux-x64.json
            ' golden "$DEPENDENCY_SOURCE"

      - name: Upload raw golden report
        if: ${{ always() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pliegors-golden-${{ matrix.environment }}-${{ inputs.tag }}-${{ github.sha }}
          path: ${{ runner.temp }}/golden-reports/*.json
          if-no-files-found: error
          retention-days: 14
          compression-level: 0

  golden_evidence:
    if: ${{ needs.golden_path.result == 'success' }}
    name: Validate exact golden environment matrix
    needs: golden_path
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Check out immutable matrix validator
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          ref: ${{ github.sha }}
          path: source

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Install exact validator dependencies
        run: npm --prefix source ci --ignore-scripts

      - name: Download all hosted golden reports
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: pliegors-golden-*-${{ inputs.tag }}-${{ github.sha }}
          path: golden-reports

      - name: Decode optional WSL2 evidence and validate matrix
        env:
          RELEASE_MODE: ${{ inputs.mode }}
          RELEASE_TAG: ${{ inputs.tag }}
          WSL_REPORT_BASE64: ${{ inputs.wsl_report_base64 }}
        run: |
          set -euo pipefail
          version="${RELEASE_TAG#v}"
          args=()
          if [ -n "$WSL_REPORT_BASE64" ]; then
            printf '%s' "$WSL_REPORT_BASE64" | base64 --decode > wsl2-x64.json
            args+=(--wsl-report wsl2-x64.json)
          elif [ "$RELEASE_MODE" = draft ]; then
            echo 'draft promotion requires the same-revision WSL2 golden report' >&2
            exit 1
          fi
          node source/scripts/check-golden-matrix.mjs \
            --input golden-reports \
            --output P8-GOLDEN-MATRIX.json \
            --commit "$GITHUB_SHA" \
            --version "$version" \
            --mode "$RELEASE_MODE" \
            "${args[@]}"

      - name: Install pinned cosign for matrix identity
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: v3.1.2

      - name: Sign and verify the validated matrix
        run: |
          set -euo pipefail
          identity="https://github.com/$GITHUB_REPOSITORY/.github/workflows/release.yml@$GITHUB_REF"
          cosign sign-blob \
            --yes \
            --bundle P8-GOLDEN-MATRIX.sigstore.json \
            P8-GOLDEN-MATRIX.json
          cosign verify-blob \
            --bundle P8-GOLDEN-MATRIX.sigstore.json \
            --certificate-identity "$identity" \
            --certificate-oidc-issuer https://token.actions.githubusercontent.com \
            P8-GOLDEN-MATRIX.json

      - name: Upload validated golden evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: pliegors-golden-matrix-${{ inputs.tag }}-${{ github.sha }}
          path: |
            P8-GOLDEN-MATRIX.json
            P8-GOLDEN-MATRIX.sigstore.json
            golden-reports/**/*.json
            wsl2-x64.json
          if-no-files-found: ignore
          retention-days: 14
          compression-level: 0

  draft:
    if: >-
      ${{
        github.ref == 'refs/heads/main' && needs.golden_evidence.result == 'success' &&
        inputs.mode == 'draft' && inputs.channel != 'canary' && inputs.confirmation == format('draft:{0}', inputs.tag)
      }}
    name: Create GitHub release draft
    needs: [golden_evidence, supply_chain]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24

      - name: Download sealed candidate
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: pliegors-sealed-${{ inputs.tag }}-${{ github.sha }}
          path: release-assets

      - name: Download verified supply-chain attestations
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: pliegors-attestations-${{ inputs.tag }}-${{ github.sha }}
          path: attestations

      - name: Download verified golden matrix
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: pliegors-golden-matrix-${{ inputs.tag }}-${{ github.sha }}
          path: golden-evidence

      - name: Verify exact release bytes again
        run: |
          node release-assets/verify-release-bundle.mjs \
            --dir release-assets \
            --expected-key-fingerprint sha256:97df5a29b5d4be6f626634b6824eebea5f2e7fcfa9c93ed644a3a2913dad7250

      - name: Create immutable manual draft
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_CHANNEL: ${{ inputs.channel }}
          RELEASE_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          if gh api "repos/$GITHUB_REPOSITORY/releases/tags/$RELEASE_TAG" \
            >/dev/null 2>release-query-error.txt; then
            echo "Release $RELEASE_TAG already exists; refusing to mutate it." >&2
            exit 1
          fi
          if ! grep -q 'HTTP 404' release-query-error.txt; then
            cat release-query-error.txt >&2
            exit 1
          fi
          if gh api "repos/$GITHUB_REPOSITORY/git/ref/tags/$RELEASE_TAG" \
            >/dev/null 2>tag-query-error.txt; then
            existing_tag_commit="$(gh api "repos/$GITHUB_REPOSITORY/commits/$RELEASE_TAG" --jq .sha)"
            if [ "$existing_tag_commit" != "$GITHUB_SHA" ]; then
              echo "Tag $RELEASE_TAG points to $existing_tag_commit, not $GITHUB_SHA." >&2
              exit 1
            fi
          elif ! grep -q 'HTTP 404' tag-query-error.txt; then
            cat tag-query-error.txt >&2
            exit 1
          fi
          version="${RELEASE_TAG#v}"
          cat > RELEASE-NOTES.md <<EOF
          PliegoRS $RELEASE_TAG is a $RELEASE_CHANNEL release of the Rust-native framework
          for verifiable, replayable, durable web interfaces.

          Highlights:

          - deterministic static generation, typed views, event logs, folds, and snapshots;
          - Rust/WASM browser clients with explicit lifecycle adapters;
          - maintained default, minimal, editorial, and cinematic starters;
          - Linux x86_64 and ARM64 production artifacts;
          - macOS and Windows development artifacts;
          - reproducible two-replica builds and a signed exact-set release manifest.

          Install the Rust CLI from the registry with
          \`cargo install pliego-cli --version $version --locked\`, or download the
          installer and platform archive attached to this release.

          The detached Ed25519 manifest covers every downloadable installer,
          verifier, framework source archive, reproducibility record, checksum,
          and CLI archive. Separate Sigstore bundles cover supply-chain and
          complete golden-environment evidence.

          PliegoRS is pre-1.0 software. Review the changelog, supported targets,
          and security policy before production use.
          EOF
          prerelease_flag=()
          if [ "$RELEASE_CHANNEL" = 'beta' ]; then
            prerelease_flag+=(--prerelease)
          fi
          gh release create "$RELEASE_TAG" \
            release-assets/* \
            attestations/* \
            golden-evidence/P8-GOLDEN-MATRIX.json \
            golden-evidence/P8-GOLDEN-MATRIX.sigstore.json \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "PliegoRS $RELEASE_TAG" \
            --notes-file RELEASE-NOTES.md \
            --draft \
            "${prerelease_flag[@]}" \
            --latest=false
